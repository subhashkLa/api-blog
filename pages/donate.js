import Layout from "../components/Layout";

const Donate = ({ paymentIntent }) => (
  <Layout>
    <div className="container">
      <h2>Donate Money</h2>
      <form>
        <label>Amount</label>
            
      </form>
    </div>
  </Layout>
);

export default Donate;
